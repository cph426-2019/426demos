import * as React from "react";
import ReactDOM from "react-dom";

export let main = () => {
    // Establish Target Element in DOM
    // Render JSX using ReactDOM's render Method
};
window.addEventListener("load", main);